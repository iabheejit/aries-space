# Version History
| Version | Date | Milestone | Changes | Files Modified |
|---------|------|-----------|---------|----------------|
| v0.1.0 | 2026-08-15 | 1 | Repo init, Mr Fox scaffold, Milestone 1 plan (READY after Vikram re-review) | CLAUDE.md, .claude/** |
| v1.0.0 | 2026-08-15 | 1 | MVP built end-to-end: pass prediction, SatNOGS ingestion, dashboard, API; 7-auditor review complete, PROCEED, all non-blocking findings fixed pre-merge | app/**, tests/**, requirements.txt, .claude/docs/** |
| v1.1.0 | 2026-08-15 | 2 | Production hardening, dashboard redesign, live-data provenance, and local container proof complete; remote CI pending | app/**, tests/**, scripts/**, Dockerfile, compose.yml, README.md, .github/** |
| v2.0.0 | 2026-08-15 | 3 | Aries storage foundation: PostgreSQL/Alembic, MinIO provenance, authenticated ingestion, MissionOps compatibility, recovery and rollback proof | services/**, tests/**, scripts/**, compose.yml, Dockerfile, README.md, .github/** |
| v2.0.1 | 2026-08-15 | 2 | M2 resolved SUPERSEDED by M3; git remote configured (private iabheejit/aries-space); merged milestone/1 + milestone/3 into main; first remote CI run caught and fixed a false-positive multi-process check in container-smoke | .github/workflows/test.yml, .claude/milestones.md, .claude/audit-trail.md |
| v2.1.0 | 2026-08-16 | 4 | TRL 5 gate reached: added sentinel2-ndvi-summary workload (real Sentinel-2 crop -> real non-null break_even_downlink_inr_per_gb, DRF 6314.6x), generalized benchmarks.py to a workload registry, dashboard now shows latest pair across all workloads. Scope Amendment documented in milestone-4 plan per Abheejit's explicit TRL-5 instruction. 72 tests passing, CI green on milestone/4-benchmark-kernel (not yet merged to main -- M4 completion audit still pending) | services/api/aries_api/**, tests/**, requirements.txt, Dockerfile, README.md, EVIDENCE.md, .claude/plans/milestone-4-benchmark-kernel.md |
| v2.2.0 | 2026-08-16 | 4 | Milestone 4 COMPLETED: 7-agent interim audit (no blockers) + fixes (fetch timeout/logging, dashboard workload label, TRL5 framing, numpy/rasterio pins for Python 3.11 CI) + AC9 closure (Playwright dashboard screenshots as CI artifacts, Sentinel-2 fixture-backed compose-smoke coverage) + two Rajan tech-debt nits. All 9 original acceptance criteria verified, CI green (test 3.11/3.13 + container-smoke), merged to main | services/api/aries_api/**, tests/**, scripts/compose_smoke.py, compose.yml, .github/workflows/test.yml, .env.example, requirements.txt |
| v2.2.1 | 2026-08-16 | 5 | Milestone 5 plan drafted (Stage 4: Expand Evidence Inputs), Vikram REFINE -> fixed (AOI-based eligibility instead of broken source-only check, watch-folder split to a follow-on milestone) -> READY. Branch created, no code yet | .claude/plans/milestone-5-expand-evidence-inputs.md, .claude/milestones.md |
| v2.3.0 | 2026-08-16 | 5 | Milestone 5 COMPLETED: roadmap Stage 4 exit gate closed -- four real workload types (satnogs-payload-anomaly-proxy, sentinel2-ndvi-summary, cloud-mask, ship-detect) across three real data sources (SatNOGS + two distinct Sentinel-2 scenes), AOI-scoped dataset eligibility, 7-agent audit (no blockers) + fixes (direct fail-closed AOI test, human-readable AOI names on dashboard). 89 tests passing, CI green, live-verified against a freshly reset Compose stack, merged to main | services/api/aries_api/**, tests/**, scripts/compose_smoke.py, compose.yml, EVIDENCE.md |
| v3.0.0 | 2026-08-16 | 6 | Milestone 6 COMPLETED: Measured-Edge AI Case Study -- real trained ONNX land-cover classifier, genuinely executed and independently timed on both ground (multi-thread) and edge-measured-mac (single-thread-constrained) targets, `is_simulated=False` both sides. `WorkloadSpec` extended with 7 optional edge-override fields (zero regression to 4 pre-existing workloads). Recommendation label renamed `simulated_edge`->`edge` project-wide. 7-agent audit (6 as agents, 1 completed by Mr Fox after an agent hit the spend limit) -- fixed inline: variable shadowing, dashboard TIMING badge honesty gap. 97 tests passing, live-verified against rebuilt Compose stack; CI run on this diff still pending | services/api/aries_api/**, scripts/train_landcover_classifier.py, tests/**, requirements.txt, requirements-dev.txt, EVIDENCE.md |
| v3.0.1 | 2026-08-16 | 6 | TCO fix: edge compute is no longer modeled as free (electricity-only). Edge targets now carry an amortized hardware CAPEX rate (default INR 300,000 / 43,800 hours = INR 6.85/hr), disclosed in every BenchmarkPair.assumptions blob. Recommendation didn't flip under default assumptions (edge still cheaper per-hour than ground's cloud rental), but break-even is no longer structurally guaranteed at 0 by construction -- computed sensitivity: flips to ground above ~INR 98/hr edge rate. 98 tests passing (new regression guard added) | services/api/aries_api/config.py, benchmarks.py, .env.example, tests/test_benchmarks.py, EVIDENCE.md |
| v3.0.2 | 2026-08-16 | 6 | Dashboard upgraded to the real product-UI concept: line-item cost breakdown (compute/energy/hardware/downlink-saved/total-per-insight) per workload, plus a computed sensitivity readout (break-even hardware rate, break-even downlink price, confidence). All derived client-side from existing API fields -- no backend/DB changes. Existing Playwright honesty-label test caught a wording regression (abbreviated "EST. HARDWARE" silently dropped the literal word "ESTIMATED" the test checks for) -- fixed by spelling it out, not by weakening the test. 100 tests passing | services/api/aries_api/templates/dashboard.html |
| v3.1.0 | 2026-08-16 | 7 | Placement Frontier started: two new real workloads deliberately NOT detection/summary tasks -- sentinel2-lossless-recompress (real zlib-9 recompression, DRF 1.05x) and sentinel2-quicklook-thumbnail (real block-mean decimation + zlib-6, DRF 15.89x) -- land genuinely near the boundary instead of the existing 28x-100x detection cluster. Computed sensitivity shows the recompress workload's break-even hardware rate (Rs 627.60/hr) is ~50-150x closer to flipping than the detection cluster's (Rs 23k-105k/hr), the frontier concept validated with real numbers. 109 tests passing, live-verified against a freshly rebuilt Compose stack | services/api/aries_api/workloads/sentinel2_lossless_recompress.py, sentinel2_quicklook_thumbnail.py, benchmarks.py, tests/**, scripts/compose_smoke.py, EVIDENCE.md |
| v3.2.0 | 2026-08-16 | 7 | TCO fix round 2 (duty cycle): researched real satellite payload duty-cycle patterns (cited CubeSat: 1 orbit/day vs Planet-scale ~100/day), found edge hardware CAPEX was still amortized against active compute-time rather than expected uses -- understated true per-run cost for a low-duty-cycle payload. Fixed: CAPEX now amortized against EDGE_EXPECTED_RUNS_PER_DAY (default 1, the conservative researched reference point). Result: every workload with a positive recommendation flipped edge->ground at default assumptions (Rs164.38/run fixed hardware charge dominates at these crop sizes). Break-even utilization (runs/day) now the dashboard's core sensitivity metric. 107 tests passing, live-verified | services/api/aries_api/config.py, metrics.py, benchmarks.py, templates/dashboard.html, tests/test_benchmarks.py, EVIDENCE.md |
| v3.3.0 | 2026-08-17 | 7 | Predicted crossover CONFIRMED live: re-tested raw network throughput (0.2MB/s -> 3.26MB/s, environment had genuinely improved), ingested the real unclipped 10,980x10,980 Sentinel-2 tile (384,030,932 bytes, 3-minute fetch), ran sentinel2-ndvi-summary against it. Real measured result: edge wins by Rs27.82/run (break-even downlink Rs427.56/GB, break-even utilization 0.86 runs/day) -- within 2% of the prior day's PREDICTED calculation. Surfaced and fixed a real operational gap: SENTINEL2_CROP_*/FETCH_TIMEOUT, BENCHMARK_TIMEOUT_SECONDS, and SENTINEL2_BENCHMARK_SAMPLE_ITERATIONS were silently un-wired in compose.yml (set in .env, never reached the container) -- now proper passthroughs. Core thesis now has real measured confirmation on both sides of the frontier: small/infrequent -> ground, full-resolution -> edge. 107 tests passing after reverting to standard fixture mode | compose.yml, EVIDENCE.md |
| v3.3.1 | 2026-08-17 | 7 | Full-tile confirmation for 4 of the 5 remaining workloads: ingested a second full-resolution scene (coastal/JNPT, dataset 5, 180.4MB single-band, ship-detect's own AOI, never fetched at full scale before) alongside the existing agricultural full tile. Real results: cloud-mask and sentinel2-quicklook-thumbnail confirm edge (Rs27.69 and Rs16.06 margins); sentinel2-lossless-recompress confirms ground (near-1x DRF never clears the fixed hardware charge, break-even downlink Rs8,378/GB). New finding: ship-detect has the highest DRF measured this session (69,580.68x) but still recommends ground -- its 180MB single-band input's downlink savings (~Rs90) don't clear the flat Rs164.38 edge hardware charge, showing DRF alone doesn't determine placement at scale, absolute input size matters too. sentinel2-landcover-classifier NOT measured -- reproducibly crashes the app container at full-tile scale (~2GB feature-array allocation across 120M pixels exceeds the local Docker VM's shared 7.65GiB ceiling), disclosed as a genuine local memory constraint and logged as a follow-up, not worked around silently | compose.yml, EVIDENCE.md |
| v3.3.2 | 2026-08-17 | 7 | Edge hardware evidence pivot: retired the unacquired Jetson Nano as the planned real-hardware target, replaced with a Raspberry Pi 4B (2GB RAM) the founder already owns. Built and dry-run-verified (not yet run on the actual Pi) scripts/pi_landcover_benchmark.py (standalone driver for real, unconstrained ONNX inference on the Pi itself) and scripts/ingest_pi_benchmark_result.py (turns the Pi's raw timing into real cost figures via the existing unmodified calculate_run_metrics()). No new hardware-measured numbers yet -- explicitly logged as groundwork, next step is running it on the physical Pi in a later session | scripts/pi_landcover_benchmark.py, scripts/ingest_pi_benchmark_result.py, EVIDENCE.md |
<!-- v{major}.{minor}.{patch}: major=milestone, minor=progress, patch=fix -->
