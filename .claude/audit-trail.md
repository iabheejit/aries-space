### Plan Review — Milestone 6
**Reviewer**: Vikram (Principal EM, 15yr)
**Status**: REFINE
**Objective Clarity**: Clear — names the exact evidence gap (every edge number today is `SIMULATED`), the exact fix (one genuinely-executed edge target via ONNX on the same host), and ties directly to the founder's stated ask this session ("actual numbers for something"). No ambiguity about what "done" looks like at the objective level.
**Acceptance Criteria Assessment**: Objective, scope, dependencies, and fundability framing are all solid — this is a REFINE, not a REWORK_PLAN. Five concrete gaps must close before coding:
1. **The core dispatch mechanism is unstated.** `_execute_target`'s non-simulated branch unconditionally calls `spec.run(payload)` for *any* non-simulated target — there's no per-target selection logic. With two non-simulated targets (ground-cpu, edge-measured-mac), the plan needs an explicit rule (`spec.edge_run(payload) if target.slug == spec.edge_target_slug else spec.run(payload)`) or the "genuinely executed, not simulated" claim in AC3 can silently fail (both targets running the ground function, timings identical) — a worse honesty problem than the `SIMULATED` label it's meant to replace.
2. **AC2 field-count inconsistency**: lists 7 fields but Approach text says "six new optional fields"; also omits how the DB's non-nullable `slowdown_factor` column on `ExecutionTarget` gets populated for the new edge row.
3. **Circular accuracy claim risk in AC5**: the model's input features (NDVI, brightness) and its weak labels come from the same spectral-index thresholds, so "held-out accuracy" is near-100% by construction, not a meaningful validation. The plan discloses this only as a training-script comment — AC5 should require the caveat appear explicitly in `EVIDENCE.md` itself.
4. **`assumptions_snapshot()` in `benchmarks.py` is workload-agnostic** and hardcodes `edge_sim_watts`/`edge_sim_slowdown_factor` into every persisted `BenchmarkPair`, which would mislabel the new workload's evidence record. Plan doesn't address this.
5. **Timeout sizing unaddressed**: `BENCHMARK_TIMEOUT_SECONDS=10s` covers warm-up + N sample iterations, single-threaded on the edge path — untested against a full-resolution crop's real ONNX inference cost.
No hidden-network-call or DB-constraint violations found — `power_source="estimated"` is a valid value under the existing CHECK constraint, and weight files are committed (no runtime fetch), so both founder-flagged risks are structurally sound as described. (All five fixed pre-coding; see the plan's Revision Note and the PM/Security audits below for verification.)

### Security Audit — Milestone 6
**Auditor**: Priya (Principal AppSec, 16yr)
**Status**: PASS
Reviewed the full changed surface: `services/api/aries_api/benchmarks.py`, `config.py`, `metrics.py`, `workloads/sentinel2_landcover_classifier.py`, `ml_models/landcover_mlp.onnx`, `scripts/train_landcover_classifier.py`, `requirements.txt`/`requirements-dev.txt`, `services/api/aries_api/templates/dashboard.html`, plus `main.py` to confirm the auth boundary.
- **Auth/attack surface**: the new workload is only reachable via the existing `POST /api/benchmarks` endpoint, still gated by `Depends(require_admin)` — no new routes, no auth regression.
- **Model-loading risk**: `MODEL_PATH` in `workloads/sentinel2_landcover_classifier.py` is a fixed repo-relative path, never touched by request input — no code path where untrusted input reaches ONNX model deserialization. The only untrusted input (the Sentinel-2 raster payload) flows through the same checksum-verified, `rasterio.MemoryFile` pattern already used by `cloud_mask`/`sentinel2_ndvi_summary`.
- **Fail-closed consistency**: `eligible_sources`/`eligible_aoi_ids` on the new `WorkloadSpec` match the existing restrictive pattern in `benchmarks.py`; no bypass.
- **Dispatch fix verified**: `_execute_target` now explicitly branches on `target.slug == "ground-cpu"` vs. `edge_run`, raising `BenchmarkUnavailableError` on a missing `edge_run` — closes the exact silent-fallback risk Vikram flagged. `assumptions_snapshot(spec)` is now spec-aware and persists real measured-edge assumptions rather than stale simulated values.
- **Supply chain**: `onnxruntime==1.23.1` (runtime), `onnx==1.22.0`/`scikit-learn==1.7.1` (dev-only, correctly excluded from the runtime image, not importable by the app) — all legitimate, exact-pinned, no typosquat risk.
- **Dashboard/Jinja2**: no `|safe` filters added; the `edge`/`ground` target lookup and `power_source` badge are autoescaped and sourced from server-controlled config/DB, not user input.
- **Error handling**: `/api/benchmarks` still routes all exceptions through typed `HTTPException`s with no stack-trace leakage.
- **Secrets**: `EDGE_MEASURED_WATTS` follows the existing env-var pattern in `config.py`; grep for secrets across new files found nothing.
No fixes required.

### PM Audit — Milestone 6
**Auditor**: Kavitha (Sr. TPM, 14yr)
**Status**: ON_TRACK
**Criteria Scorecard**:
  - AC1 (real trained ONNX model, committed weights, reproducible training script, registered in `WORKLOAD_REGISTRY`): PASS — `scripts/train_landcover_classifier.py` reads the real fixture crop, weak-labels via disclosed spectral thresholds, trains an `sklearn.MLPClassifier`, hand-builds a Gemm→Relu→Gemm→Softmax ONNX graph and runs `onnx.checker.check_model` before saving. `services/api/aries_api/ml_models/landcover_mlp.onnx` is committed (binary, confirmed present and readable). Registered in `WORKLOAD_REGISTRY` in `benchmarks.py` (lines 159-181) with `eligible_sources`/`eligible_aoi_ids` matching the AOI-scoped pattern of the other Sentinel-2 workloads.
  - AC2 (`WorkloadSpec` 7-field edge override, existing 4 workloads unaffected): PASS — exactly 7 new optional fields on `WorkloadSpec` (`edge_run, edge_target_slug, edge_target_name, edge_power_source, edge_model_version, edge_avg_watts, edge_is_simulated`), all defaulted to reproduce today's `edge-sim` behavior. Test `test_existing_workloads_still_use_formula_derived_edge_sim` (tests/test_benchmarks.py:302) is a real regression guard, not a rename — it asserts `cloud-mask` still seeds `ground-cpu`+`edge-sim`, `power_source=="simulated"`, `model_version=="edge-sim-m4-v1"`, and that `edge_measured_watts` does NOT leak into its assumptions blob. Confirms zero regression on the 4 pre-existing workloads, matching Vikram's fix #2.
  - AC3 (both targets run same model via onnxruntime under different real `SessionOptions`, `is_simulated=False` both, honest measured/estimated labels): PASS — `_ground_session()` uses default `SessionOptions`; `_edge_constrained_session()` sets `intra_op_num_threads=1`, `inter_op_num_threads=1`, `ORT_SEQUENTIAL` — a genuine, not cosmetic, constraint. `_execute_target`'s non-simulated branch explicitly dispatches `spec.run` for `ground-cpu` and `spec.edge_run` for anything else, raising `BenchmarkUnavailableError` if a non-simulated non-ground target has no `edge_run` (Vikram's fix #1, verified present at benchmarks.py:316-332). Test `test_edge_session_is_actually_single_threaded` independently asserts the ONNX session options rather than trusting the code path.
  - AC4 (Apple-Silicon-stand-in honesty label): PASS — `edge_target_name="Measured Edge Stand-in (Apple Silicon, single-core constrained)"` in the registry; `run_edge_constrained()`'s docstring and the workload's `payload()["limitations"]` field both state this is a resource-constrained host-CPU stand-in, not space-qualified hardware; dashboard template conditionally renders "Edge timing is measured, not modeled" vs. the old simulated copy based on `power_source`.
  - AC5 (EVIDENCE.md: architecture, held-out accuracy, measured ground vs. edge, recommendation/break-even, circularity caveat verbatim per fix #3): PASS — confirmed directly in `EVIDENCE.md` lines 78-93 (not just code comments): architecture stated (4-feature → 8-unit hidden MLP → 3-class softmax), held-out accuracy stated (100%, train 99.96%) immediately followed by an explicit, unambiguous circularity disclosure ("training labels are weak labels derived from the same spectral-index thresholds... used as model input features, so this accuracy figure validates that the MLP reproduces the threshold rule... it is not independent ground-truth land-cover validation"), real ground (0.783ms) vs. edge (0.878ms) wall-clock with recommendation (`edge`) and break-even (₹0.00/GB) numbers, and an honestly-reported nuance that edge was measured *slower* than ground at this crop size with a labeled synthetic (not part of the committed pair) exploratory check at 1024×1024 for context.
  - AC6 (64+ test suite green; new tests for determinism/thread-constraint/shape-validity/backward-compat): PASS — both named tests exist and match their described intent (`test_landcover_classifier_produces_two_genuinely_measured_runs` at tests/test_benchmarks.py:274, `test_existing_workloads_still_use_formula_derived_edge_sim` at tests/test_benchmarks.py:302). `tests/test_landcover_classifier_workload.py` independently covers determinism, ground/edge output agreement, single-thread session-options assertion, malformed-input rejection, and output-size reduction. Founder-reported local run: 97 passed, 5 skipped, no failures — consistent with a full green suite plus the new coverage; I did not re-run pytest myself in this audit but the test bodies match their claimed assertions line-for-line, which is the more important check.
  - AC7 (compose_smoke.py exercises new workload end-to-end against live stack): PASS — `scripts/compose_smoke.py` (lines 159-174) POSTs `/api/benchmarks?workload=sentinel2-landcover-classifier` against the real running Compose stack, asserts `201`, asserts both `ground-cpu`/`edge-measured-mac` target slugs present, asserts `class_pixel_fraction` in both results, asserts `edge_measured_watts` in the assumptions blob. Founder independently ran this against a freshly rebuilt stack and got a clean `{"status": "ok", ...}` including `landcover_classifier_benchmark_pair_id` — real live verification, not just a unit test claiming it.
**Scope Creep**: One item worth naming, not blocking: the recommendation field's string value was renamed project-wide from `"simulated_edge"` to `"edge"` (API, dashboard template, tests) — this was not listed as an acceptance criterion or named in the plan's Approach section, and it changes the output contract for all 5 workloads, not just the new one (any external consumer, dashboard screenshot, or grant document that quotes the literal string `"simulated_edge"` from prior milestones is now stale against the live API). It is, however, a small, mechanically necessary consequence of AC3/AC4 (the old name asserted "formula-derived" as a universal truth that a measured-edge workload breaks), it is disclosed openly and explained in EVIDENCE.md itself (line 93), and all touched call sites (metrics.py, dashboard.html, test_metrics.py, test_benchmarks.py) were updated consistently — no orphaned references to the old value found in code. Estimated added scope: under 1 hour, low risk. Recommend adding one sentence to Milestone 6's plan record after the fact so future audits don't have to reconstruct this from the diff. No other creep: no space/Jetson hardware code, no dashboard visual redesign, no RF/telecom workload — the plan's Out of Scope boundary held.
**Missed Deliverables**: None against the 7 ACs as written. One disclosed, non-blocking gap: the plan's Approach section describes training against "the existing real fixture... (or the full ingested crop)" — the committed evidence run used the 128×128 fixture, not a larger live-fetched crop, because a 1024×1024 live-AWS attempt hit HTTP 206 partial-read network flakiness in this environment. This is disclosed in EVIDENCE.md itself, not hidden, and does not change the core measured-vs-simulated claim (both targets still genuinely execute and are timed at whatever crop size is used) — see Demo/Report Readiness below for why this doesn't block sign-off.
**Technical Debt Introduced**: Low. (1) `ExecutionTarget.slowdown_factor` is populated with the unused global default for measured-edge targets because the DB column is non-nullable — documented in three places (plan Revision Note, code comment at benchmarks.py:267-271, Vikram's review) as intentionally-ignored dead data rather than a silent trap; acceptable, well-labeled. (2) The "water" class is dropped from the 4-class ontology implied by the milestone's own Objective section ("vegetation / water / cloud / bare-soil") down to 3 classes, with the honest reason given (zero water pixels in the training crop) in both the training script and the workload's `limitations` field — this is a disclosed scope reduction, not a hidden shortcut, but it means the milestone's own Objective text (still 4 classes) is now inconsistent with the delivered model (3 classes); worth a one-line correction to the plan file for the record. (3) No hardcoded secrets, no generic 500s, no silent fallbacks — confirmed by both my read and Priya's independent security audit reaching the same conclusion.
**Demo/Report Readiness**: Yes, demo-ready today, with one sentence of prep. The core claim — a real trained model, genuinely executed and timed twice under two different real thread constraints on the same host, both MEASURED not modeled — holds regardless of crop size; the AWS-fetch network flakiness only affects which crop was used for the committed number, and that limitation is disclosed in EVIDENCE.md in the same paragraph as the numbers, not buried or omitted. This is exactly the honest-disclosure pattern this project has kept consistent since M4 (SIMULATED/ESTIMATED labels) and it strengthens rather than undermines credibility with a technical funder — a reviewer who checks EVIDENCE.md will see disclosed limitations next to real numbers, not a polished number with a hidden asterisk. Recommend the founder have a one-line answer ready if asked about crop size: "128×128 committed pixel crop; larger live-fetch run planned once network path is unconstrained, timing methodology is identical at any size."
**Recommendations**: Fix now — none, milestone is clean to sign off. Wait/follow-up (non-blocking, log as tracked items not urgent fixes): (1) retry the 1024×1024 live-AWS crop once network access allows, to get a larger committed number matching the exploratory synthetic check already described in EVIDENCE.md; (2) one-line correction to the Milestone 6 plan file noting the 3-class (not 4-class) delivered ontology and the `simulated_edge`→`edge` rename, purely for audit-trail accuracy; (3) add `onnxruntime` to whatever dependency-scanning/Dependabot process exists, per Priya's note, before this becomes a second forgotten native-code dependency.
**Next Session Should Start With**: Milestone 6 is closed. Next session should read `.claude/milestones.md` to confirm M6 is marked COMPLETE, then move to whatever M7 planning is queued (per the "Aries end-goal roadmap" — likely the next roadmap stage after benchmark kernel + measured-edge evidence). No re-litigation of M6 needed; the two follow-up items above (larger crop retry, plan-file correction) are low-priority and should not block M7 kickoff.

### Design Audit — Milestone 6
**Auditor**: Divya (Principal UX, 11yr)
**Status**: ITERATE
**User Flow Assessment**:
  - Primary action path: Dashboard is read-only (no form/CTA funnel) — the "action" for this audience (grant reviewer, investor) is comprehension: can they correctly conclude, at a glance, "this edge number is real, not simulated"? No extra clicks/steps introduced by this diff; the generalized `rejectattr`/`recommendation == 'edge'` lookups render in the same single-pass template load as before. No new friction in navigation.
  - Drop-off risks: None in the click-through sense (nothing to abandon on a read-only card). The real risk is a *comprehension* drop-off: a skimming reviewer reads the visual badges, not the footnote prose, and the badges currently do not distinguish this milestone's headline claim from the old simulated-only workloads (see Information Hierarchy below).
**Information Hierarchy**: Primary claim is buried, not obvious. The card's one strong visual signal is `<span class="badge {{ run.power_source }}">{{ run.power_source | upper }}</span>` — red for `simulated`, amber for `estimated`. For the new workload, `ground-cpu` is `power_source="estimated"` (as always) and the new `edge-measured-mac` target is *also* `power_source="estimated"` (per `benchmarks.py` line 177, `edge_power_source="estimated"`). Result: both columns render an identical amber "ESTIMATED" badge, with no third badge state ("MEASURED") ever existing in the CSS or template. The one fact this milestone exists to prove — genuinely measured edge timing, as opposed to formula-derived — is stated only in a single sentence inside `<span class="assumptions">`, which is deliberately de-emphasized styling (`color:var(--muted)`, `font-size:.78rem`, appended after the recommendation paragraph). A reviewer who scans badges (the fastest, most natural read of a card like this) sees no visual difference between this workload's card and the four pre-existing simulated ones. This inverts the intended hierarchy: the strongest evidence in the whole grant case is rendered with the *weakest* visual weight on the page built to showcase it.
**Feedback & Error States**:
  - Success feedback: N/A (no user action to confirm) — but see Information Hierarchy: the "this is real" signal that should function as success/trust feedback for a skeptical viewer is present in text only, not reinforced visually.
  - Error messages: Unchanged empty/error paths (`{% else %}<p class="empty">{{ benchmark_error }}</p>{% endif %}`) are untouched by this diff and remain reasonable — server-supplied message, not a raw exception.
  - Loading/empty states: Unaffected by this change; no new loading state introduced (server-rendered page).
**Consistency Check**: Two inconsistencies introduced by this diff:
  1. Terminology drift — the two mutually-exclusive footnote branches use different words for the same concept ("Edge figures are **simulated**" vs. "Edge timing is **measured, not modeled**"). A first-time reader building a mental model across workloads has to infer that "modeled" ≈ "simulated," since the two branches never appear together and the word is never defined. Recommend using the same vocabulary in both branches, e.g.: *"Edge timing here is measured from real runs, not simulated; power for both sides is still an estimate."* — mirrors "simulated" from the sibling branch instead of introducing "modeled" as a synonym.
  2. Target-name length — `<h3>{{ run.target_name }}</h3>` has held to short 2-3 word labels for every target so far ("Terrestrial CPU", "Simulated Edge Node"). The new `edge_target_name`, "Measured Edge Stand-in (Apple Silicon, single-core constrained)" (66 chars, 8 words), breaks that pattern by a wide margin and will visibly wrap to multiple lines in the `minmax(260px,1fr)` target column (`.target h3 { font-size:1rem }`), making the edge card taller and visually mismatched against the ground card's one-line heading in the same row.
**Accessibility Gaps**: None new. Badge color-coding is still paired with a text label (`SIMULATED`/`ESTIMATED`), not color alone, so colorblind/screen-reader users aren't dependent on hue — an existing good pattern this diff doesn't regress. Long `h3` text wrapping is a layout/hierarchy issue (below), not a screen-reader issue — heading text is still read correctly, just verbosely.
**Mobile/Responsive**: Appropriate at the breakpoint level — `@media(max-width:900px)` already collapses `.comparison` to a single column, so the long heading just adds vertical height rather than breaking layout. Not a regression, but the extra height compounds the existing hierarchy problem: on mobile the long h3 pushes the badge and metrics further down the scroll before a viewer reaches any numbers at all.
**Redesign Targets** (none rise to full redesign — both are targeted, low-effort fixes):
  - `templates/dashboard.html` badge logic: add a genuine third visual state so "measured" timing is visible without reading footnote prose — e.g., a small inline tag next to (not replacing) the power-source badge, keyed off `edge.power_source == 'estimated' and benchmark.recommendation` context or a dedicated boolean the backend already effectively has via `spec.edge_is_simulated`/`is_simulated`. Even a plain-text prefix on the h3 kicker ("MEASURED · edge-measured-mac") would outperform the current all-footnote approach for a skimming reader.
  - `benchmarks.py` `WORKLOAD_REGISTRY` entry, `edge_target_name`: shorten the h3-facing string to something scannable, e.g. `"Edge Stand-in (Apple Silicon)"`, and move the AC4-required "single-core constrained, not space-qualified hardware" disclosure into the assumptions footnote text instead — that text is currently the *only* place carrying this required honesty disclosure, so it must be preserved somewhere when the h3 is shortened, not dropped.
**Recommendations** (ranked by impact):
  1. Highest impact: give "measured" its own visible signal on the card (not just prose) — this is the single fact the milestone exists to prove, and it's currently the least visually prominent thing on the page. Cheapest fix: change badge text/color when `is_simulated=False` on the edge side to something distinct from "ESTIMATED" (e.g. a green "MEASURED" chip alongside the existing power-source badge), rather than overloading one badge field to carry two different facts (timing-provenance and power-provenance) that happen to collide into the same word for this workload.
  2. Unify "simulated" vs. "not modeled" wording across the two footnote branches so the reader learns one vocabulary, not two, for the same concept.
  3. Shorten `edge_target_name` for the h3 heading; relocate the "not space-qualified hardware" caveat (required by AC4) into the footnote/body copy rather than the heading, so honesty is preserved with better scannability.
  4. None of the above block shipping this milestone's actual deliverable (real measured numbers exist and are correctly computed/persisted per Priya's security audit and Vikram's plan review) — these are copy/label-level fixes, estimate under an hour of template + registry-string changes, no data model or backend logic touched.

### Strategic Audit — Milestone 6
**Auditor**: Meera (Venture Partner, 2x Founder, 20yr)
**Status**: ALIGNED
**Project Context Discovered**: Pre-seed/grant-stage deep-tech venture, solo-founder-plus-agent-team operating cadence, one active user (the founder, demoing to grant reviewers), racing a DoT/TCOE India Pilot Grant submission with TRL 5 already reached (2026-08-16) and entity-registration/financials/LoI paperwork now the acknowledged critical path. Five milestones shipped in roughly 36 hours of session time — MVP, storage foundation, benchmark kernel (TRL5 gate), evidence-breadth expansion, and now this measured-edge case study — all narrowly targeted at grant defensibility rather than broad product-market building.
**Venture Impact**: A genuinely trained, genuinely dual-executed ONNX model (`sentinel2-landcover-classifier`) replaces the single weakest evidentiary claim in the grant packet — every prior "edge wins" number was a ground median times a configured slowdown constant. This milestone produces two real wall-clock numbers (0.783ms multi-thread vs 0.878ms single-thread-constrained, same model, same input, same host) with `is_simulated=False` on both sides, plus an honestly reported nuance that edge was *slower* at this crop size and only wins economically on compute-rent, not latency. That nuance, reported rather than hidden, is itself a credibility asset — it is exactly the kind of finding that reads as scientifically serious to a technical grant reviewer, more so than a clean "edge always wins" number would. Divya's design audit independently confirms the flip side of this same finding: the dashboard's badge system doesn't yet visually surface "measured" vs. "simulated" (both currently render an identical amber "ESTIMATED" badge), so the strongest evidence in the packet is currently under-signaled on the one surface a skimming reviewer actually looks at — a real, low-cost gap worth closing before a live reviewer demo, not a reason to withhold ALIGNED status here.
**Strategic Leverage**: High. This directly answers the founder's own explicit, named ask this session ("actual numbers for something... not estimated or simulated") and was scoped, reviewed (Vikram REFINE, five real gaps closed pre-code, not rubber-stamped), executed, and evidenced inside roughly one day against a hard external deadline. It also converts a documented technical-debt risk (per Vikram's plan review: touching shared dispatch/`_seed_catalog`/`assumptions_snapshot` core, not just adding another isolated workload) into a working, tested, spec-driven extension mechanism — done deliberately, sequenced, and regression-tested against the four pre-existing workloads rather than improvised.
**Cross-Project Reuse**: Reusable within Aries's own roadmap, not yet cross-project. The `edge_run`/`edge_target_slug` override on `WorkloadSpec` is a generic "plug in any genuinely-executed non-ground target" mechanism — adding a real space/Jetson partner node or a telecom edge server later is additively the same shape of change (new `ExecutionTarget` row + a real `run` callable), not a rewrite. That plausibly reduces future rework for both strategic threads the founder raised this session (space AI data centers for cloud/telecom providers; edge-capable-minority-of-satellites framing) — but neither thread is built yet, and the mechanism is proven at "one Mac, two thread configs" scale, not at "heterogeneous real infrastructure" scale. Treat the reuse claim as directionally sound, not yet load-tested.
**Fundability Assessment**:
  - Active pipeline relevance: Directly serves the single live funding effort (DoT/TCOE Pilot Grant). This is the strongest evidentiary artifact in the packet as of this milestone — a real trained model, real dual timing, real honesty about a counter-intuitive result — and it was built in response to feedback the founder gave about a specific weakness reviewers would likely probe.
  - Investor/funder narrative: Strengthens it, and does so by demonstrating scientific discipline (disclosing the circular weak-label accuracy caveat, disclosing that edge was slower at small scale, disclosing the 128x128-vs-1024x1024 scope reduction as an environment limitation rather than silently shipping a smaller number as if it were the intended one) rather than overselling. This is the kind of self-critical rigor that differentiates a credible TRL5+ pilot case from a pitch-deck claim, and it is worth naming explicitly in the next grant narrative/update, not just leaving buried in EVIDENCE.md.
**Sustainability Check**: Team (founder + agent specialists) can operate and extend this without new dependencies beyond `onnx`/`onnxruntime`, both already network-installed and pinned in the image; no new infrastructure, no new external partner dependency introduced. The mechanism degrades gracefully — all four pre-existing workloads are provably unaffected (explicit regression tests), so this doesn't destabilize what already worked for the grant's TRL5 evidence.
**Builder's Trap Warning**: Mild, named, and defensible rather than ignored. This did touch shared core execution logic (dispatch, catalog seeding, assumptions snapshot) rather than being a pure additive registry entry like Milestone 5's two workloads — Vikram's plan review named this risk explicitly and the team responded by sequencing regression tests against the existing workloads before layering the new model on top, rather than treating the risk as invisible. The one place this could tip into a builder's trap is the deferred 1024x1024 full-resolution run: chasing a bigger, more visually impressive number by fighting the sandbox's network flakiness would be effort spent on presentation polish, not evidentiary substance — the 128x128 result already satisfies the founder's actual ask (measured, not modeled). Recommend treating the larger crop as optional polish, not a blocking follow-up, unless a specific reviewer requirement demands it.
**Founder Decision Needed**: (1) Whether the 128x128 measured-edge result is sufficient for the grant submission as-is, or whether a genuine 1024x1024 run (from an unconstrained network, not this sandbox) is worth founder time before the deadline — recommend treating it as optional, not blocking. (2) Whether to proactively address, in the grant narrative itself, the gap between "recommend edge" and "most real satellites are sensor-only with no onboard compute" (raised in conversation this session, not yet reflected anywhere in EVIDENCE.md or the dashboard's recommendation language) — leaving it unaddressed risks a reviewer surfacing it as a gotcha; framing it upfront as "this benchmarks a design-time placement decision for edge-capable payloads" turns a potential weakness into evidence of the team understanding its own scope. (3) Whether to spend the ~1 hour Divya identified (badge visual distinction for measured-vs-simulated, terminology unification, h3 length) before the next live reviewer demo — small cost, directly protects this milestone's core value from being missed by a skimming reader.
**Recommendations**: Do not build the "AI data centers in space" or "sensor-only satellite" threads as new milestones yet — they are strategic framing/narrative decisions, not code, and the grant clock is the binding constraint. Spend the next cycle on the founder-side paperwork blockers already identified in the session log (entity registration, financials, GHRCE LoI, itemized budget) and on writing the grant narrative paragraph that presents this milestone's honest "edge is sometimes slower, wins on cost not always latency" finding as a strength — that paragraph is worth more marginal fundability right now than any further engineering on this benchmark. Fold in Divya's cheap "MEASURED" badge fix opportunistically (under an hour, no backend change) since it directly protects this milestone's fundability value on the one surface reviewers will actually look at. Keep the `edge_run` mechanism as-is otherwise; it does not need preemptive generalization until a real second edge target (partner hardware) exists to prove it against.

### DevOps Audit — Milestone 6
**Auditor**: Sanjay (Platform/SRE, 12yr)
**Status**: OPERATIONAL_RISK
**Deployability**:
  - Deploy process: automated (`docker compose up --build --detach --wait`, single-worker Uvicorn behind Compose, non-root container, unchanged mechanism this milestone).
  - Environment config: documented. New `EDGE_MEASURED_WATTS` var follows the existing `_float_env` pattern and is present in `.env.example`; `BENCHMARK_TIMEOUT_SECONDS`/`SENTINEL2_BENCHMARK_SAMPLE_ITERATIONS` were already documented there. No dev/prod separation concern introduced (single-environment Compose deployment, consistent with project stage).
  - Rollback path: exists at container/image level (no schema migration this milestone; `ml_models/landcover_mlp.onnx` is committed and version-pinned to the git revision, so rollback is just "redeploy the previous commit's image") — sound in principle, but not exercised as a drill for this milestone specifically.
**Observability**:
  - Logging: sparse for the new workload — `sentinel2_landcover_classifier.py` has zero log statements. Failures do surface as typed exceptions (`BenchmarkUnavailableError` etc.), caught and logged with `correlation_id` at the `run_benchmark_pair` level, so a *failure* is traceable, but there is no on-success log line (model version, thread count actually applied, class distribution) to confirm at 3am that an edge-measured run genuinely used 1 thread without re-deriving it from the persisted `assumptions` JSON. Acceptable at this stage (single admin-gated internal tool) but worth adding before this becomes a live demo people rely on.
  - Health endpoints: present, unchanged (`/health/live`, `/health/ready`), exercised by CI's `container-smoke` job and the Dockerfile `HEALTHCHECK`. The onnxruntime model load happens at workload-module import time, not at startup/readiness-check time — if `landcover_mlp.onnx` were ever missing or corrupted, that would only surface on the first benchmark request, not at `/health/ready`. Low probability (552-byte committed file, not fetched) but currently silent until exercised.
  - Alerting/monitoring: none — acceptable at this stage (internal grant-evidence tool, no live external users; nothing new missing relative to project baseline).
**Failure Behaviour**:
  - Downstream dependency failure: N/A for onnxruntime itself — model is local/committed, no runtime network fetch, consistent with the project's "no hidden network calls" posture. Existing MinIO/Postgres failure paths are unchanged and already fail closed with typed errors rather than crashing the process.
  - Error visibility: meaningful. `run_benchmark_pair` wraps the whole benchmark pair in try/except, rolls back the DB session, cleans up orphaned MinIO objects (logging cleanup failures via `logger.exception`), and re-raises typed, client-safe exceptions (`DatasetNotFoundError`, `DatasetIneligibleError`, `BenchmarkUnavailableError`) rather than leaking stack traces. `_execute_target_bounded`'s timeout wrapper raises a `BenchmarkUnavailableError` naming the specific offending target slug. The new workload inherits this pattern with no special-casing needed — confirmed by reading `benchmarks.py:309-345, 365-379, 539-552`.
**CI/CD Pipeline**: partial gap, not a hard blocker. `.github/workflows/test.yml`'s `test` job matrix (`python-version: ["3.11", "3.13"]`) installs `requirements-dev.txt`, which pulls in `onnxruntime==1.23.1` transitively — onnxruntime 1.23.x publishes wheels for cp310–cp313 across manylinux/macOS/Windows, so both matrix legs should resolve a prebuilt wheel without a source build; no workflow change is strictly required for Python-version compatibility. However: (1) this milestone has evidently **not yet gone through actual CI** — `.claude/session-log.md`'s most recent entry is still Milestone 5 — so the developer's local `docker build`/`compose up --wait`/`compose_smoke.py` success (Docker Desktop, Apple Silicon host) is real but is not equivalent proof that `container-smoke`'s `ubuntu-latest` (x86_64) runner resolves and runs the identical wheel; risk is low (onnxruntime x86_64 manylinux wheels are extremely well-established) but unverified until CI actually runs on this diff. (2) No fail-fast smoke assertion (e.g. `python -c "import onnxruntime"`) exists to give a legible one-line CI failure if a future onnxruntime version ever lacks a wheel for a matrix Python version — today that would instead surface confusingly deep inside pytest collection or a slow/failing source build.
**Backup & Recovery**: documented and unaffected by this milestone (existing Postgres/MinIO backup-restore procedure from Milestone 2 is untouched). The new binary artifact (`landcover_mlp.onnx`) is itself trivially recoverable — reproducible via the committed `scripts/train_landcover_classifier.py`, and at 552 bytes git history alone is sufficient; no object-storage/LFS concern.
**Operational Debt**: (1) No on-success log line for the new workload. (2) CI has not yet run against this milestone's actual diff — "existing test suite still green" is currently a locally-verified claim only, on a different CPU architecture than CI uses, for a newly-added native-code dependency. (3) The benign `onnxruntime cpuid_info` warning (`Unknown CPU vendor. cpuinfo_vendor value: 0`) will appear in every container's stdout going forward — cosmetic, but will read as alarming to a future on-call engineer unfamiliar with it; a one-line comment near session creation would prevent a false-alarm investigation later.
**Recommendations** (ranked by incident probability):
  1. Run this milestone through the real `container-smoke` CI job (`ubuntu-latest`, cp3.13) before merge — closes the only genuinely unverified technical gap (x86_64 wheel resolution under the exact CI environment); low effort, happens automatically on push.
  2. Add a one-line INFO log on successful benchmark completion for `sentinel2-landcover-classifier` (model version, thread count actually applied) — cheap, prevents "prove it happened" debugging later given this is the project's headline grant evidence.
  3. Add a short code comment near the onnxruntime session-creation code noting the `cpuid_info`/`Unknown CPU vendor` warning is expected/benign under Docker Desktop virtualization.
  4. (Low priority, pre-existing pattern) Consider a fail-fast `import onnxruntime` smoke step in CI's `test` job so a future wheel-resolution regression fails legibly rather than deep inside pytest/source-build.
**Minimum Before Production**: Item 1 above (a real CI run of `container-smoke` against this exact diff) should complete before this milestone is marked done. Confirmed independently: the benchmark timeout math holds with wide margin — `_execute_target_bounded` (`benchmarks.py:365-379`) wraps the entire warmup-plus-timed-sample loop (`min(100, iterations)` warmup calls at `benchmarks.py:334` + `iterations` timed calls at `benchmarks.py:338`, all inside one `ThreadPoolExecutor.result(timeout=config.BENCHMARK_TIMEOUT_SECONDS)` call) in a single 10s budget; at the measured worst case (~26ms/call, 1024×1024 synthetic scale), 30 total calls (15 warmup + 15 timed) costs roughly 0.8s — over 12x headroom, no timeout risk even accounting for CI-runner slowness. Everything else in this entry is advisory given the project's current stage (internal grant-evidence tool, single admin user, no live external traffic).

### Engineering Audit — Milestone 6
**Auditor**: Arjun (Staff SWE, 13yr)
**Status**: NEEDS_WORK
**Readability Assessment**:
  - Naming clarity: Mostly clear and consistent with the existing 4 workload modules (`WORKLOAD_SLUG`, `DETECTOR_VERSION`, `*Result` frozen dataclass with `.payload()`, `run()`, `canonical_result_bytes()` — same shape as `cloud_mask.py`/`sentinel2_ndvi_summary.py`/`ship_detect.py`). One real finding: `benchmarks.py::_seed_catalog(session, spec)` reassigns its own `spec: WorkloadSpec` parameter inside `for spec in target_specs:` (line ~276), shadowing it with a plain `dict`. No functional bug today (the shadowed value isn't read again in that function), but it's a live footgun — the next engineer adding a third seeded target or reading a stack trace mid-loop will see `spec` bound to the wrong type. Rename the loop variable (`target_spec`) — trivial fix, flag before it bites someone under time pressure.
  - Function/class size: `_execute_target` (`benchmarks.py`, ~60 lines) is doing more than it used to (simulated branch, ground-cpu branch, generic-edge branch, result packaging) but is still readable in about 15-20 seconds because the three branches are clearly commented and each stays on-topic. Not a redesign candidate yet, but it's the piece most likely to need splitting if a third execution-target category is ever added. `run_benchmark_pair` remains the largest function in the file (~150 lines) — pre-existing size, not grown disproportionately by this milestone's diff.
  - Code cohesion: `sentinel2_landcover_classifier.py` is a coherent unit — session caching, feature extraction, classification, and payload serialization are each a small single-purpose function. `train_landcover_classifier.py` is similarly coherent as an offline script (labels → features → train → export, one `main()` chaining clearly-named helpers).
**Test Quality**:
  - Coverage of logic paths: Adequate. `test_landcover_classifier_workload.py` covers valid class distribution/sums-to-1, determinism, ground/edge prediction agreement, single-thread session-options assertion, single-band rejection, and output-size reduction. `test_benchmarks.py` adds two targeted regression tests for the new `WorkloadSpec` edge-override mechanism (both dispatch directions).
  - Test fidelity — verified both flagged tests directly:
    - `test_edge_session_is_actually_single_threaded` (workload test): calls `_edge_constrained_session()` and asserts `options.intra_op_num_threads == 1` and `inter_op_num_threads == 1`. ONNX Runtime's `SessionOptions` defaults both fields to `0` ("let the runtime choose", typically multi-core) — removing `options.intra_op_num_threads = 1` from `_edge_constrained_session()` would leave the value at its `0` default, and `0 == 1` fails. This is a real, non-tautological regression guard, not a check against its own trivially-true implementation.
    - `test_existing_workloads_still_use_formula_derived_edge_sim` (benchmarks test): runs `cloud-mask` end-to-end and asserts `target_slug == {"ground-cpu","edge-sim"}`, `power_source == "simulated"`, `model_version == "edge-sim-m4-v1"`, and `assumptions["edge_sim_watts"]` present / `edge_measured_watts` absent. If the `WorkloadSpec` defaults for the 4 pre-existing workloads were broken (e.g., `edge_is_simulated` flipped, or `edge_run` left unset while `is_simulated` were somehow `False`), `_execute_target`'s non-simulated branch would raise `BenchmarkUnavailableError` (no `edge_run` configured) and the test would fail via unhandled exception before the assertions even ran — this is a genuine end-to-end regression guard on the shared dispatch mechanism, not a check that merely restates the code under test.
  - Gap worth noting: `test_landcover_classifier_produces_two_genuinely_measured_runs` verifies `power_source`, `model_version`, and the `edge_measured_watts` assumptions key, but never asserts on the `execution.timing_basis` field that `_execute_target` actually sets (`"measured_native_constrained_median_estimated_power"` vs. `"modeled_from_ground_median"`), nor that `wall_ms`/`inference_ms` are plausible independently-measured values rather than a formula. In practice this is a low-severity gap — `power_source`/`model_version` are set directly from the same `WorkloadSpec` fields that gate the dispatch branch, so the current code can't drift the two independently — but the test would read as directly proving AC3's central "genuinely measured, not modeled" claim if it also asserted `timing_basis`.
  - Test maintainability: readable, well-named (`test_existing_workloads_still_use_formula_derived_edge_sim` states its own regression intent in the name), fixtures (`multi_aoi_benchmark_context`, `crop_bytes`) are reused consistently with the existing suite, no brittle mocking — tests run the real ONNX model against the real committed fixture crop.
**Duplication & Consistency**:
  - Feature-engineering logic (NDVI formula, brightness, `/10000.0` normalization, and the `[red, nir, ndvi, brightness]` feature order/stacking) is duplicated verbatim between `sentinel2_landcover_classifier.py::_read_features()` (inference) and `train_landcover_classifier.py::_features()` (training) — two independent implementations of the same math with no shared function and no test that pins them to each other. Currently they agree (verified by inspection), but this is exactly the kind of duplication that silently diverges: if a future edit changes normalization, feature order, or the NDVI epsilon-handling in one file and not the other, the ONNX model's learned weights would be fed differently-scaled/ordered inputs at inference than at training time, producing silently-wrong (but structurally valid — same output shape, same class set) predictions. No existing test would catch this, since the workload tests only check output shape/determinism/class-set validity, not that predictions are sane relative to training-time feature semantics. Recommend extracting a single shared `_landcover_features(red, nir) -> np.ndarray` used by both files (or at minimum, importing from one into the other) before this drifts.
  - Spectral-index thresholds (`CLOUD_REFLECTANCE_THRESHOLD = 2000.0`, `VEGETATION_NDVI_THRESHOLD = 0.3`) are re-declared as fresh literals in `train_landcover_classifier.py` rather than imported from `cloud_mask.py`/`sentinel2_ndvi_summary.py`, where the same constants already live. The training script's own comment discloses this ("same thresholds already used by...") but doesn't enforce it — if `cloud_mask.CLOUD_REFLECTANCE_THRESHOLD` changes, the training script's weak labels silently stop matching the workload it claims to mirror, with nothing to flag the drift. Lower severity than the feature-duplication finding above (weak-label thresholds are explicitly disclosed as a soft heuristic, not a correctness-critical inference path), but still an avoidable divergence risk — import the constants instead of retyping them.
  - Otherwise the new workload module's structure (registry pattern, dataclass/payload shape, `canonical_result_bytes`) is consistent with all 4 existing workloads — no stylistic divergence.
**CI/CD Hygiene**: `.github/workflows/test.yml` runs the full pytest suite plus `compileall` on two Python versions, then a separate container-smoke job that rebuilds the Docker image, boots the real Compose stack, and exercises the new workload end-to-end via `compose_smoke.py` (confirmed: asserts `target_slug` set, `class_pixel_fraction` in result, `edge_measured_watts` in assumptions) plus a Playwright dashboard pass. This milestone's new code is fully exercised by CI's intended pipeline. Agreeing with Sanjay's DevOps audit: this milestone had not, at time of review, actually been run through GitHub Actions on this exact diff — local success on an Apple Silicon host is real evidence but not equivalent to the `ubuntu-latest` x86_64 runner CI actually uses; recommend confirming a green `container-smoke` run before calling this done, though the risk itself is low (onnxruntime publishes standard manylinux wheels). Separate, pre-existing, repo-wide gap not introduced by this milestone: no `ruff`/`mypy`/`flake8`/`black`/pre-commit config anywhere — `compileall` catches syntax errors only, not type errors or lint issues (e.g., the `_seed_catalog` shadowing below would not be caught by any automated check today). Log as ongoing tech debt, not a milestone-6-specific blocker.
**Dead Code / TODOs**: None found in any of the 4 audited files (no `TODO`/`FIXME`/`XXX` markers, no unreferenced functions — `run`, `run_edge_constrained`, `_ground_session`, `_edge_constrained_session`, `_classify`, `_read_features`, `canonical_result_bytes` are all called from either the workload module itself or `benchmarks.py`/tests).
**Dependency Split**: Verified correct. `sentinel2_landcover_classifier.py` imports only `json`, `dataclasses`, `pathlib`, `typing`, `numpy`, `onnxruntime`, `rasterio` — no `onnx` or `sklearn` import anywhere in the runtime package (grepped `services/api/aries_api/`, zero matches). `onnxruntime==1.23.1` correctly lives in `requirements.txt` (runtime); `onnx==1.22.0` and `scikit-learn==1.7.1` correctly live only in `requirements-dev.txt`, explicitly commented `# Offline-only: scripts/train_landcover_classifier.py (not imported by the app)`. The split matches actual import reality, not just the comment's claim.
**Refactor Targets** (ordered by cost-of-change, cheapest first):
  1. `services/api/aries_api/benchmarks.py::_seed_catalog` — rename the loop variable `spec` (shadowing the function's `WorkloadSpec` parameter) to `target_spec`. Five-minute fix, zero risk, removes a real readability trap.
  2. `services/api/aries_api/workloads/sentinel2_landcover_classifier.py::_read_features` + `scripts/train_landcover_classifier.py::_features`/`_weak_labels` — extract shared feature-engineering (and ideally the spectral thresholds) into one importable location so training and inference cannot silently diverge. Medium cost (touches the committed model's provenance story slightly — worth doing as a fast-follow, not blocking this merge since current behavior is verified correct).
  3. (Optional, low priority) Add a `timing_basis` assertion to `test_landcover_classifier_produces_two_genuinely_measured_runs` so the test directly proves the "genuinely measured, not modeled" claim at the dispatch level, not just via the correlated `power_source`/`model_version` fields.
**Recommendations**: Ship the workload/tests as-is — no correctness bugs found, all acceptance criteria (AC1-AC7) are met, both specifically-flagged tests are real regression guards (verified by reasoning through what would happen if the guarded line were removed), and the runtime/dev dependency split is genuinely correct, not just asserted. Fix #1 (variable shadowing) before merge if convenient — it's trivial. Log #2 (feature-engineering duplication between training and inference) and #3 (timing_basis test gap) as tech debt for a fast-follow; they're real divergence risks but the current code is correct and low-churn (an offline training script, a committed model, and a two-endpoint case-study workload — not a path under active iteration), so they don't rise to REFACTOR/blocking severity for this grant-deadline milestone. Status is NEEDS_WORK rather than CLEAN specifically because of the `_seed_catalog` shadowing (cheap, should fix) and the feature-duplication divergence risk (real, should track) — neither blocks merge, and Sanjay's independent finding that this diff hasn't yet run through real CI on the target architecture should also be closed before calling the milestone fully done.
**Post-audit fix applied**: `_seed_catalog`'s loop variable renamed `spec` → `target_spec` per Arjun's finding #1, immediately, before consolidation.

### Architecture Audit — Milestone 6
**Auditor**: Rajan's assigned agent hit the session's monthly spend limit mid-review and was terminated before it could write its findings — its analysis is genuinely lost, not merely delayed (confirmed: no partial section exists in this file). Rather than leave the milestone one specialist short or re-attempt a spawn that would likely hit the same limit, Mr Fox is completing this review directly, reasoning through the same brief Rajan was given. Flagged as a substitution, not attributed to Rajan.
**Status**: SOUND
- **WorkloadSpec backward compatibility**: confirmed structurally, not just by inspection — `test_existing_workloads_still_use_formula_derived_edge_sim` (tests/test_benchmarks.py) exercises `cloud-mask` end-to-end post-change and asserts identical seeded targets, `power_source`, and `model_version` to pre-milestone behavior. All 7 new `WorkloadSpec` fields default to values that reproduce the old single-target-shape "Simulated Edge Node" row exactly; the 4 pre-existing `WORKLOAD_REGISTRY` entries construct `WorkloadSpec` without touching any of the 7 new fields, so they're unaffected by construction, not by convention.
- **Dispatch correctness**: `_execute_target`'s non-simulated branch resolves `run_fn` explicitly on `target.slug == "ground-cpu"` vs. else-`spec.edge_run` (raising `BenchmarkUnavailableError` on a missing `edge_run`), closing the exact silent-fallback failure mode Vikram's plan review flagged (both targets would otherwise run `spec.run` and produce numerically indistinguishable "measured" results under a false honesty label). Verified by reading the branch directly, not by trusting the docstring.
- **Session singleton thread-safety** (`sentinel2_landcover_classifier.py`'s module-level `_GROUND_SESSION`/`_EDGE_SESSION`, lazily constructed): each `_execute_target_bounded` call runs inside its own single-worker `ThreadPoolExecutor`, so two concurrent `run_benchmark_pair` calls (e.g. two simultaneous API requests) could each observe `None` and construct a redundant `onnxruntime.InferenceSession` before the module-level global settles — wasted construction work, not a correctness bug: Python's GIL serializes the attribute write, and any thread reading the global mid-race gets either the old or new session object, both fully valid and interchangeable (same model, same options). No use-after-free or partial-init exposure. Worth a `functools.lru_cache`-style guard only if concurrent benchmark load becomes real (not now — this is a low-traffic grant-demo endpoint, not a hot path).
- **Committing a 552-byte ONNX binary to git**: sound for this project's current scale and philosophy (small, deterministic, versioned alongside the code that depends on it — consistent with how the two Sentinel-2 fixture `.tif` files are already committed). Would reconsider only if model artifacts grow into MB/GB range or need independent versioning from application code; neither applies here.
- **`simulated_edge` → `edge` rename / dashboard generalization** (`rejectattr('target_slug', 'equalto', 'ground-cpu')` replacing the hardcoded `selectattr(..., 'edge-sim')`): a correct generalization for the current 2-target-per-pair invariant, and the `by_slug[spec.edge_target_slug]` lookup in `run_benchmark_pair` makes the same assumption explicit rather than hidden. Flagging one latent constraint for the record, not a defect: both the dashboard's `rejectattr` and the kernel's `ground = by_slug["ground-cpu"]` / `edge = by_slug[spec.edge_target_slug]` pairing assume exactly one non-ground target per pair. Nothing in this milestone breaks that invariant, but if a future workload ever needs a 3-way comparison (e.g., ground vs. two distinct edge profiles), both call sites would need revisiting together — worth a one-line comment at the `by_slug` construction noting the 2-target assumption, as a signpost for whoever adds target #3.
**Recommendation**: No blocking issues. SOUND to merge. The one signpost-comment suggestion above is optional polish, not a fix-before-ship item.

### CTO Consolidated — Milestone 6
**Date**: 2026-08-16
**Mr Fox's Call**: PROCEED
**Executive Summary**: Milestone 6 closes the weakest evidence gap in the grant case — every prior edge number was formula-derived (ground-baseline median × configured slowdown factor); this milestone adds a real trained ONNX land-cover classifier, genuinely executed and independently timed twice (multi-thread "ground" vs. single-thread-constrained "edge" stand-in), both `MEASURED` not modeled, verified live against the rebuilt Compose stack. Six of seven specialist audits ran as agents; the seventh (architecture) was completed directly by Mr Fox after its agent hit the account's monthly spend limit mid-review, clearly labeled as a substitution in the trail above. One trivial code fix (variable shadowing) and one dashboard honesty fix (a distinct MEASURED/SIMULATED timing badge, since power-source alone couldn't distinguish the new workload's genuinely-measured edge from the four workloads' formula-simulated edge) were applied post-audit, before this consolidation, and re-verified against a fresh Docker rebuild.
**From Priya (Security)**: PASS — no fixes required; dispatch-fallback risk from plan review confirmed closed; no auth/injection/supply-chain issues.
**From Kavitha (PM)**: ON_TRACK — all 7 acceptance criteria verified against code, not claims; one disclosed scope item (`simulated_edge`→`edge` rename) flagged as reasonable, not creep; demo-ready today.
**From Rajan (Architecture — completed by Mr Fox after agent failure)**: SOUND — backward compatibility and dispatch correctness verified structurally; session-singleton race is benign (wasted construction, not a correctness bug); one optional signpost-comment suggestion, not blocking.
**From Meera (Strategy)**: ALIGNED — genuinely strengthens the grant case; honest counter-intuitive disclosure (edge measured *slower* than ground at small crop size) is a credibility asset, not a weakness; two founder-only decisions flagged (pursue larger live crop before submission? address sensor-only-satellite framing proactively in the narrative?).
**From Arjun (Engineering)**: NEEDS_WORK → fixed inline — `_seed_catalog` variable-shadowing fix applied; feature-engineering duplication between train/inference scripts and a missing `timing_basis` test assertion logged as tracked tech debt, not blocking.
**From Divya (Design)**: ITERATE → fixed inline — added a distinct TIMING MEASURED/SIMULATED badge (previously both ground and the new edge target showed an identical "ESTIMATED" power badge, burying the milestone's core claim in footnote text), unified "modeled" vs. "simulated" vocabulary, shortened the edge target's dashboard name (full Apple-Silicon-stand-in caveat preserved in the workload's own limitations text).
**From Sanjay (DevOps)**: OPERATIONAL_RISK — one real open item: this diff has not yet run through GitHub Actions on the `ubuntu-latest`/x86_64 runner (only verified locally on Apple Silicon); risk assessed low (onnxruntime publishes standard manylinux wheels) but genuinely unverified until CI runs.
**Blocking Issues**: None. All specialist-flagged blocking-adjacent items were fixed inline during this consolidation (variable shadowing, dashboard badge honesty gap) and re-verified via full local test suite (97 passed, 5 skipped) plus a fresh `docker compose up --build` + `compose_smoke.py` run (clean `{"status": "ok", ...}`, including the new `landcover_classifier_benchmark_pair_id`).
**Action Items**:
- [ ] Push this branch and confirm a green `container-smoke` CI run on `ubuntu-latest` before treating this as fully proven (Sanjay's flag) — the one real unverified item.
- [ ] Retry the 1024×1024 live-AWS-fetch crop once network access is unconstrained, for a larger committed case-study number (current committed run uses the 128×128 fixture crop; disclosed in EVIDENCE.md, not blocking).
- [ ] Fast-follow: extract shared feature-engineering code between `sentinel2_landcover_classifier.py` and `train_landcover_classifier.py` so they cannot silently diverge (Arjun's finding #2).
- [ ] Founder decision needed: whether to proactively address in the grant narrative that "edge wins" is a design-time/edge-capable-payload claim, not universal across the sensor-only majority of real satellites (Meera's flag, echoes this session's earlier strategic discussion).
**Mr Fox's Note to Abheejit**: This is real, demo-ready evidence — a trained model, executed and timed twice under two different real thread constraints on the same machine, both genuinely measured. The honestly-reported nuance (edge measured slower than ground at small scale, faster only projected at scale) is worth leading with in front of a technical reviewer, not hiding — it's exactly the kind of result a formula can't produce and proves this wasn't rigged to say what you wanted to hear. Two things need your call before this goes in the deck: push to CI for the last verification gap, and decide how upfront to be about the sensor-only-satellite constraint in the narrative. Six of seven audits ran clean or fixed-inline; the account's spend limit interrupted one agent mid-review, which I completed directly and labeled honestly rather than paper over.

### Milestone 7 — Placement Frontier (Started): Mr Fox Self-Review
**Date**: 2026-08-16
**Process note, stated plainly**: this milestone skipped the full plan-draft → Vikram review → 7-agent completion audit ceremony used for Milestones 1-6. Two reasons, both disclosed rather than silently deviated: (1) this session already hit the account's monthly agent-spend limit once during Milestone 6's audit, losing one specialist's review entirely — spawning another 7-9 agents for a smaller, lower-risk addition risked repeating that failure; (2) the two new workloads strictly follow the exact `WorkloadSpec`/`WORKLOAD_REGISTRY` pattern already reviewed and passed by Vikram, Priya, Rajan, Arjun, Kavitha, Divya, and Sanjay across Milestones 4-6 — no new architectural surface, no new dependencies, no new execution-target mechanism, no DB schema change.
**Mr Fox's own check against what a completion audit would verify**:
- Security: no new routes, no new auth surface, no untrusted-input parsing (zlib/numpy operate only on the same checksum-verified raster bytes every other Sentinel-2 workload already processes); no secrets, no new dependencies (zlib is stdlib, numpy already a dependency).
- Architecture: both workloads use the existing `WorkloadSpec` fields with no new optional fields added to the dataclass; `canonical_result_bytes` returning raw compressed bytes instead of a JSON-wrapped summary is a legitimate, already-supported pattern (the function was always workload-defined, not required to wrap `.payload()`) -- verified by inspection, not assumption.
- Engineering/test fidelity: `test_output_is_genuinely_decompressible_to_original_pixels` and `test_uniform_block_downsamples_to_the_same_uniform_value` both independently verify the real computation (decompression round-trip; a uniform input must decimate to the same uniform value) rather than re-asserting the implementation's own output — real regression guards, not tautologies.
- PM/scope: matches exactly what was scoped after the founder's explicit choice ("wait for new near-boundary workloads" — chose to build genuinely new workloads now, consistent with that stated preference, not the deferred sweep-existing-workloads alternative).
- Honesty: DRF numbers (1.05x, 15.89x) were measured after implementation, not chosen in advance — verified in this conversation's tool output before being written into EVIDENCE.md or this file.
**Status**: PROCEED. No blocking issues identified. Flagged for a real (agent-based) audit pass once account spend limits reset, particularly Priya (security) and Rajan (architecture) for independent confirmation — logged as a tracked follow-up, not a blocker, given the low-risk nature of this addition.
